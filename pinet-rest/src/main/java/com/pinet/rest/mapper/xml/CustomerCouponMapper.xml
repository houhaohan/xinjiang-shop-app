<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pinet.rest.mapper.CustomerCouponMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.pinet.rest.entity.CustomerCoupon">
        <result column="id" property="id"/>
        <result column="create_by" property="createBy"/>
        <result column="create_time" property="createTime"/>
        <result column="update_by" property="updateBy"/>
        <result column="update_time" property="updateTime"/>
        <result column="del_flag" property="delFlag"/>
        <result column="coupon_grant_id" property="couponGrantId"/>
        <result column="coupon_grant_record_id" property="couponGrantRecordId"/>
        <result column="customer_id" property="customerId"/>
        <result column="expire_time" property="expireTime"/>
        <result column="coupon_name" property="couponName"/>
        <result column="coupon_type" property="couponType"/>
        <result column="coupon_status" property="couponStatus"/>
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        cc.id,
        cc.create_by,
        cc.create_time,
        cc.update_by,
        cc.update_time,
        cc.del_flag,
        cc.coupon_grant_id, cc.coupon_grant_record_id, cc.customer_id, cc.expire_time, cc.coupon_name, cc.coupon_type, cc.coupon_status
    </sql>


    <select id="selectCustomerCouponList" resultType="com.pinet.rest.entity.vo.CustomerCouponVo">
        select
        cc.id,
        cc.customer_id,
        cc.coupon_id,
        cc.expire_time,
        cc.coupon_status,
        c.name as couponName,
        c.type as couponType,
        c.use_price as thresholdAmount,
        c.coupon_price as couponAmount,
        c.use_shop,
        c.discount,
        c.use_vip
        from customer_coupon cc
        left join coupon c on c.id = cc.coupon_id
        ${ew.customSqlSegment}
    </select>


    <select id="selectCustomerCouponVoById" resultType="com.pinet.rest.entity.vo.CustomerCouponVo">
        select
        cc.id,
        cc.customer_id,
        cc.coupon_id,
        cc.expire_time,
        cc.coupon_status,
        c.name as couponName,
        c.type as couponType,
        c.use_price as thresholdAmount,
        c.coupon_price as couponAmount,
        c.use_shop,
        c.discount
        from customer_coupon cc
        left join coupon c on c.id = cc.coupon_id
        where cc.id = #{id}
    </select>

</mapper>
