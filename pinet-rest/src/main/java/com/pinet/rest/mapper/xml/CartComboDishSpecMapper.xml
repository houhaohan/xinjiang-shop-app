<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pinet.rest.mapper.CartComboDishSpecMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.pinet.rest.entity.CartComboDishSpec">
        <result column="id" property="id" />
        <result column="create_by" property="createBy" />
        <result column="create_time" property="createTime" />
        <result column="update_by" property="updateBy" />
        <result column="update_time" property="updateTime" />
        <result column="del_flag" property="delFlag" />
        <result column="cart_id" property="cartId" />
        <result column="shop_prod_id" property="shopProdId" />
        <result column="shop_prod_spec_id" property="shopProdSpecId" />
        <result column="shop_prod_spec_name" property="shopProdSpecName" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id,
        create_by,
        create_time,
        update_by,
        update_time,
        del_flag,
        cart_id, shop_prod_id, shop_prod_spec_id, shop_prod_spec_name
    </sql>

    <select id="getByUserIdAndShopProdSpecId" resultType="com.pinet.rest.entity.vo.CartComboDishSpecVo">
        select c.id as cartId, 
        c.shop_prod_id,
        c.shop_id,
        ccd.shop_prod_id as singleProdId,
        ccds.shop_prod_spec_id,
        ccds.shop_prod_spec_name
        from cart c
        inner join cart_combo_dish ccd on ccd.cart_id = c.id and ccd.del_flag = 0
        inner join cart_combo_dish_spec ccds on ccds.shop_prod_id = ccd.shop_prod_id and ccds.del_flag = 0
        where c.customer_id = #{userId} and ccds.shop_prod_spec_id in
        <foreach collection="shopProdSpecIds" item="item" separator="," open="(" close=")">
            #{item}
        </foreach>
        and c.del_flag = 0
    </select>

</mapper>
