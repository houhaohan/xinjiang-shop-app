server:
  port: 8090
spring:
  application:
    name: pinet-rest
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    druid:
      driver-class-name: com.mysql.cj.jdbc.Driver
      url: jdbc:mysql://127.0.0.1:3306/xjs?autoReconnect=true&useUnicode=true&characterEncoding=utf8&zeroDateTimeBehavior=convertToNull
      username: root
      password: 123456
      min-idle: 10
      max-pool-prepared-statement-per-connection-size: 20
      validation-query-timeout: 30000
      test-while-idle: true
      time-between-eviction-runs-millis: 5000
      max-active: 400
      validation-query: SELECT 1 FROM DUAL
      max-wait: 60000
      connection-properties: druid.stat.mergeSql=true;druid.stat.slowSqlMillis=5000
      test-on-borrow: false
      pool-prepared-statements: true
      initial-size: 20
      min-evictable-idle-time-millis: 300000
      test-on-return: false

  redis:
    database: 0
    host: 127.0.0.1
#    password: qwer1234
    password:
    port: 6379



mybatis-plus:
  mapper-locations: classpath*:com/pinet/mapper/xml/*.xml
  configuration:
    ## 自动驼峰命名规则映射
    map-underscore-to-camel-case: true
    auto-mapping-behavior: full
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  #实体类扫描
  global-config:
    banner: false
    # 逻辑删除配置
    db-config:
      ## 逻辑删除字段
      logic-delete-field: del_flag
      ## 配置逻辑删除字段为1是删除
      logic-not-delete-value: 0
      ## 逻辑删除未删除值
      logic-delete-value: 1

wxpay:
  #应用编号
  appId: xxxx
  #商户号
  mchId: xxx
  # APIv2密钥
  apiKey: xxxx
  # APIv3密钥
  apiV3Key: xxx
  # 微信支付V3-url前缀
  baseUrl: https://api.mch.weixin.qq.com/v3
  # 支付通知回调, pjm6m9.natappfree.cc 为内网穿透地址
  notifyUrl: http://pjm6m9.natappfree.cc/pay/payNotify
  # 退款通知回调, pjm6m9.natappfree.cc 为内网穿透地址
  refundNotifyUrl: http://pjm6m9.natappfree.cc/pay/refundNotify
  # 密钥路径,resources根目录下
  keyPemPath: apiclient_key.pem
  #商户证书序列号
  serialNo: xxxxx

